name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  POSTGRES_DB: test
  DATABASE_URL: "postgresql://postgres:postgres@host.docker.internal:5432/test?schema=public"
  NEXTAUTH_SECRET: "test-secret-for-ci"
  NEXTAUTH_URL: "http://localhost:3000"
  AUTH_SECRET: "test-secret-for-ci"
  AUTH_URL: "http://localhost:3000"
  RESEND_APIKEY: "re_dummy_key_for_ci_build"

jobs:
  build:
    name: Build
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Start PostgreSQL
        run: |
          # Use uma porta aleatória entre 50000-60000
          POSTGRES_PORT=$((50000 + RANDOM % 10000))
          echo "Using port: $POSTGRES_PORT"
          
          docker run -d \
            --name postgres-build-${{ github.run_id }} \
            -e POSTGRES_USER=postgres \
            -e POSTGRES_PASSWORD=postgres \
            -e POSTGRES_DB=test \
            -p $POSTGRES_PORT:5432 \
            --health-cmd pg_isready \
            --health-interval 10s \
            --health-timeout 5s \
            --health-retries 5 \
            postgres:16-alpine
          
          echo "Waiting for PostgreSQL to be ready..."
          timeout 60 bash -c "until docker exec postgres-build-${{ github.run_id }} pg_isready -U postgres; do sleep 2; done"
          
          # Exporta a porta para os próximos steps
          echo "POSTGRES_PORT=$POSTGRES_PORT" >> $GITHUB_ENV

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Run database migrations
        run: |
          export DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:$POSTGRES_PORT/test?schema=public"
          echo "DATABASE_URL=$DATABASE_URL"
          npx prisma db push

      - name: Build application
        run: |
          export DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:$POSTGRES_PORT/test?schema=public"
          npm run build

      - name: Cleanup PostgreSQL
        if: always()
        run: |
          docker stop postgres-build-${{ github.run_id }} || true
          docker rm postgres-build-${{ github.run_id }} || true

  lint:
    name: Lint
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Biome
        uses: biomejs/setup-biome@v2
        with:
          version: 2.4.4

      - name: Run Biome
        run: biome ci .

  unit:
    name: Unit Tests (Vitest)
    runs-on: self-hosted
    needs: [lint]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm run test:unit

  e2e:
    name: E2E Tests (Playwright)
    runs-on: self-hosted
    needs: [lint, build, unit]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Start PostgreSQL
        run: |
          # Use uma porta aleatória entre 50000-60000
          POSTGRES_PORT=$((50000 + RANDOM % 10000))
          echo "Using port: $POSTGRES_PORT"
          
          docker run -d \
            --name postgres-e2e-${{ github.run_id }} \
            -e POSTGRES_USER=postgres \
            -e POSTGRES_PASSWORD=postgres \
            -e POSTGRES_DB=test \
            -p $POSTGRES_PORT:5432 \
            --health-cmd pg_isready \
            --health-interval 10s \
            --health-timeout 5s \
            --health-retries 5 \
            postgres:16-alpine
          
          echo "Waiting for PostgreSQL to be ready..."
          timeout 60 bash -c "until docker exec postgres-e2e-${{ github.run_id }} pg_isready -U postgres; do sleep 2; done"
          
          # Exporta a porta para os próximos steps
          echo "POSTGRES_PORT=$POSTGRES_PORT" >> $GITHUB_ENV

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Run database migrations
        run: |
          export DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:$POSTGRES_PORT/test?schema=public"
          echo "DATABASE_URL=$DATABASE_URL"
          npx prisma db push

      - name: Run Playwright tests
        run: |
          export DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:$POSTGRES_PORT/test?schema=public"
          echo "DATABASE_URL=$DATABASE_URL"
          npm run test:e2e

      - name: Upload Playwright Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: Upload Test Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: test-results/
          retention-days: 7

      - name: Cleanup PostgreSQL
        if: always()
        run: |
          docker stop postgres-e2e-${{ github.run_id }} || true
          docker rm postgres-e2e-${{ github.run_id }} || true
